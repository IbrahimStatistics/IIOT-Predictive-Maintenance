# Minimal local-dev Mosquitto config.
# Allows unauthenticated connections — fine for localhost-only dev,
# NOT suitable if this broker is ever exposed beyond your own machine.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest file /mosquitto/log/mosquitto.log
log_dest stdout
